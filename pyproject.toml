[project]
name = "research-pipeline"
version = "0.1.0"
description = "Academic paper processing pipeline — microservices for discovery, analysis, extraction, validation, and code generation."
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]
