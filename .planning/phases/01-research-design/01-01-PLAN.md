---
phase: 01-research-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [shared/__init__.py, shared/db.py, shared/models.py, shared/server.py, shared/config.py, services/.gitkeep, tests/.gitkeep, pyproject.toml, .python-version]
autonomous: true
---

<objective>
Analyze CAS microservice as reference pattern and establish the shared library foundation with module stubs.

Purpose: Create the architectural blueprint and skeleton code that all future microservices will build upon. The CAS microservice (:8769) is the proven reference — extract its good patterns, improve on its weaknesses.
Output: Working shared/ package with module stubs, pyproject.toml with dependencies, and design documented in ARCHITECTURE.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-research-design/01-CONTEXT.md

# CAS microservice reference (READ-ONLY — do not modify these files):
@/media/sam/1TB/N8N_dev/scripts/cas_microservice.py
@/home/sam/.config/systemd/user/cas-microservice.service
@/media/sam/1TB/N8N_dev/docs/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze CAS microservice and extract patterns</name>
  <files>.planning/phases/01-research-design/CAS-ANALYSIS.md</files>
  <action>
Read `/media/sam/1TB/N8N_dev/scripts/cas_microservice.py` (405 LOC) thoroughly. Extract and document:

1. **HTTP Server pattern**: How BaseHTTPRequestHandler is used, request routing, JSON parsing
2. **Endpoint structure**: /health, /validate — request/response formats, status codes
3. **Error handling**: How errors are caught, formatted, returned as JSON
4. **Configuration**: How port, timeouts, paths are configured (hardcoded vs env vars)
5. **Process management**: How subprocesses (Maxima, SageMath) are spawned and managed
6. **Logging**: What's logged, format, verbosity levels

Also read the systemd unit file at `/home/sam/.config/systemd/user/cas-microservice.service` to understand:
- Service type, restart policy
- Environment variables passed
- Working directory setup

Write findings to `.planning/phases/01-research-design/CAS-ANALYSIS.md` with sections:
- Patterns to Keep (what works well)
- Patterns to Improve (what the shared lib should do better)
- Anti-patterns to Avoid (things we should NOT replicate)

Be specific — cite line numbers and code snippets from the CAS service.
  </action>
  <verify>CAS-ANALYSIS.md exists with all 3 sections populated and references specific CAS code patterns</verify>
  <done>CAS microservice fully analyzed with patterns, improvements, and anti-patterns documented</done>
</task>

<task type="auto">
  <name>Task 2: Design and create shared library skeleton</name>
  <files>shared/__init__.py, shared/db.py, shared/models.py, shared/server.py, shared/config.py, services/.gitkeep, tests/.gitkeep, pyproject.toml, .python-version</files>
  <action>
Based on CAS analysis and CONTEXT.md vision, create the project structure and shared library skeleton.

**Directory structure:**
```
research-pipeline/
  shared/                  # Shared library
    __init__.py            # Package init, version
    db.py                  # SQLite connection management
    models.py              # Pydantic models (Paper, Formula, Validation, GeneratedCode)
    server.py              # Base HTTP server class
    config.py              # Configuration loading + dotenvx integration
  services/                # Future microservices (empty for now)
    .gitkeep
  tests/                   # Test directory
    .gitkeep
  pyproject.toml           # Project metadata + dependencies
  .python-version          # Python version pin
```

**shared/__init__.py**: Package version, convenience imports.

**shared/db.py**: Stub with docstring describing the interface:
- `get_connection(db_path)` — returns sqlite3.Connection with WAL mode, foreign keys enabled
- `init_db(db_path)` — creates tables from schema
- Connection as context manager (auto-commit/rollback)
- Do NOT implement yet — just the module docstring and empty function signatures with type hints

**shared/models.py**: Stub with Pydantic model skeletons (fields TBD in Phase 02):
- `Paper` — academic paper metadata
- `Formula` — extracted LaTeX formula
- `Validation` — CAS validation result
- `GeneratedCode` — generated Python/Rust code
- `ServiceStatus` — health/status response for /health and /status endpoints
- `ProcessRequest` / `ProcessResponse` — base request/response for /process endpoint
- Include only class names, docstrings, and `pass` bodies

**shared/server.py**: Stub with base HTTP server class:
- `BaseService(HTTPServer)` — base class all services inherit
- `BaseHandler(BaseHTTPRequestHandler)` — base request handler with JSON helpers
- Document the intended API: `send_json()`, `read_json()`, route dispatch
- Do NOT implement yet — just class skeletons with docstrings

**shared/config.py**: Stub with config loading interface:
- `load_config(service_name)` — loads config from env vars + dotenvx
- `Config` dataclass with common fields (port, db_path, log_level, service_name)
- Document dotenvx integration pattern
- Do NOT implement yet — just signatures and docstrings

**pyproject.toml**: Minimal project config:
- name: research-pipeline
- python: ">=3.11"
- dependencies: pydantic>=2.0
- dev-dependencies: pytest, pytest-asyncio
- No build system needed (not an installable package)

**.python-version**: "3.11" (match system Python)

IMPORTANT: All module files are STUBS with docstrings and empty function/class signatures.
The actual implementation happens in Phase 02 (DB), Phase 03 (HTTP server), etc.
This phase is about establishing the architecture, not writing implementation code.
  </action>
  <verify>
All files exist:
- python3 -c "import shared; print('shared package OK')" (from project root with PYTHONPATH=.)
- ls shared/*.py services/.gitkeep tests/.gitkeep pyproject.toml .python-version
  </verify>
  <done>Project structure created with shared/ package containing stub modules, pyproject.toml, and directory layout ready for Phase 02+</done>
</task>

<task type="auto">
  <name>Task 3: Write ARCHITECTURE.md design document</name>
  <files>.planning/phases/01-research-design/ARCHITECTURE.md</files>
  <action>
Write comprehensive architecture document at `.planning/phases/01-research-design/ARCHITECTURE.md` that serves as the reference for all future phases.

Sections:

1. **Overview**: One-paragraph system description, data flow diagram (ASCII)
2. **Directory Structure**: Full tree with descriptions of each directory/file purpose
3. **Module Specifications**:
   - `shared/db.py`: SQLite interface, WAL mode, connection pooling strategy, schema management approach
   - `shared/models.py`: Pydantic model hierarchy, serialization strategy, validation rules
   - `shared/server.py`: Base HTTP server design, route dispatch, JSON helpers, error response format
   - `shared/config.py`: Configuration loading, dotenvx integration, env var naming convention
4. **API Contract**: Standard endpoint specs that ALL services must implement:
   - GET /health → `{"status": "ok", "service": "name", "uptime_seconds": N}`
   - GET /status → `{"service": "name", "version": "X.Y.Z", "db_path": "...", "last_processed": "..."}`
   - POST /process → service-specific processing (request/response format per service)
   - Standard error format: `{"error": "message", "code": "ERROR_CODE", "details": {}}`
5. **Design Decisions**: Table of decisions with rationale (SQLite, http.server, monorepo, etc.)
6. **Service Port Map**: 8770-8775 assignments for each future service
7. **Data Flow**: How papers flow through the pipeline (ASCII diagram)
8. **AI Agent Integration**: How agents interact with the services (JSON contracts, error handling, idempotency)

Reference CAS-ANALYSIS.md for patterns adopted and improved.
Reference 01-CONTEXT.md for vision alignment.
  </action>
  <verify>ARCHITECTURE.md exists with all 8 sections populated, references both CAS analysis and context vision</verify>
  <done>Architecture document complete, ready to guide Phase 02-04 implementation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] CAS-ANALYSIS.md has specific code references from cas_microservice.py
- [ ] shared/ package importable: `PYTHONPATH=. python3 -c "import shared"`
- [ ] All stub modules have docstrings and type-hinted signatures
- [ ] pyproject.toml valid: `python3 -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"`
- [ ] ARCHITECTURE.md covers all 8 sections with concrete specifications
- [ ] No CAS files were modified (read-only reference)
</verification>

<success_criteria>

- CAS microservice patterns analyzed and documented
- Project directory structure established (shared/, services/, tests/)
- Shared library stubs created with clear interfaces
- ARCHITECTURE.md provides complete blueprint for Phase 02-04
- All design decisions from PROJECT.md and CONTEXT.md reflected in architecture
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-design/01-01-SUMMARY.md`
</output>
