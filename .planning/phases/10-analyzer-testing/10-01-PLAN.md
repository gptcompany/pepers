# Plan 10-01: Analyzer Service Test Suite

**Phase:** 10 — Analyzer Testing
**Date:** 2026-02-13
**Depends on:** Phase 09 (Analyzer Implementation complete)
**Wave:** 1 (single plan)

## Objective

Comprehensive test suite for the Analyzer service: unit tests with mocked LLM responses, integration tests with real SQLite + mock LLM, E2E tests with real Ollama calls. Follows Phase 7 (Discovery Testing) patterns exactly.

## Deliverables

| # | File | LOC (est.) | Description |
|---|------|-----------|-------------|
| 1 | `tests/conftest.py` | +30 | Analyzer-specific fixtures |
| 2 | `tests/unit/test_analyzer.py` | ~400 | Unit tests — prompt, llm, handler |
| 3 | `tests/integration/test_analyzer_db.py` | ~300 | Integration — real DB, mock LLM, real HTTP |
| 4 | `tests/e2e/test_analyzer_e2e.py` | ~80 | E2E — real Ollama calls |

**Total estimated:** ~810 LOC new test code

## Implementation Steps

### Step 1: tests/conftest.py — Add Analyzer Fixtures

Add to existing conftest.py:

```python
@pytest.fixture
def sample_llm_scores():
    """Valid LLM scoring response as dict."""
    return {
        "scores": {
            "kelly_relevance": 0.85,
            "mathematical_rigor": 0.70,
            "novelty": 0.60,
            "practical_applicability": 0.75,
            "data_quality": 0.65,
        },
        "reasoning": "Relevant paper on Kelly criterion with solid math.",
    }

@pytest.fixture
def sample_llm_response_json(sample_llm_scores):
    """Valid LLM JSON response string."""
    return json.dumps(sample_llm_scores)

@pytest.fixture
def sample_low_score_response():
    """LLM response with below-threshold scores."""
    return json.dumps({
        "scores": {
            "kelly_relevance": 0.2,
            "mathematical_rigor": 0.3,
            "novelty": 0.1,
            "practical_applicability": 0.2,
            "data_quality": 0.3,
        },
        "reasoning": "Paper not related to Kelly criterion.",
    })

@pytest.fixture
def discovered_paper_db(initialized_db, sample_paper_row):
    """DB with one discovered paper ready for analysis."""
    from services.discovery.main import upsert_paper
    upsert_paper(str(initialized_db), sample_paper_row)
    return initialized_db

@pytest.fixture
def migrated_db(initialized_db):
    """DB with prompt_version migration applied."""
    from services.analyzer.main import migrate_db
    migrate_db(str(initialized_db))
    return initialized_db
```

### Step 2: tests/unit/test_analyzer.py — Unit Tests (~400 LOC)

#### TestFormatScoringPrompt (6 tests)
- `test_basic_format` — title + abstract + authors + categories
- `test_truncates_authors_over_5` — >5 authors → "et al. (N total)"
- `test_missing_abstract` — None abstract → "(abstract not available)"
- `test_short_abstract` — <50 chars → "(abstract not available)"
- `test_empty_categories` — empty list → no crash
- `test_empty_authors` — empty list → no crash

#### TestStripMarkdownFences (5 tests)
- `test_json_fence` — ` ```json\n{...}\n``` ` → `{...}`
- `test_plain_fence` — ` ```\n{...}\n``` ` → `{...}`
- `test_no_fence` — `{...}` → `{...}` unchanged
- `test_whitespace_handling` — leading/trailing whitespace
- `test_nested_backticks` — backticks inside content preserved

#### TestGetGeminiApiKey (3 tests)
- `test_key_present` — returns key value
- `test_key_missing` — raises RuntimeError
- `test_key_empty_string` — raises RuntimeError

#### TestCallGeminiCli (5 tests)
- `test_success_json_response` — mock subprocess, valid JSON → returns text
- `test_non_zero_exit` — returncode=1 → RuntimeError
- `test_api_error_in_response` — `{"error": {"message": "..."}}` → RuntimeError
- `test_timeout` — subprocess.TimeoutExpired → exception propagates
- `test_strips_fences_from_response` — markdown fence in response → stripped

#### TestCallGeminiSdk (4 tests)
- `test_success` — mock genai.Client → returns text
- `test_empty_response` — response.text=None → RuntimeError
- `test_api_key_missing` — no GEMINI_API_KEY → RuntimeError
- `test_timeout` — mock raises TimeoutError → exception propagates

#### TestCallOllama (5 tests)
- `test_success` — mock urlopen → returns response text
- `test_non_200` — status 500 → RuntimeError
- `test_error_in_response` — `{"error": "..."}` → RuntimeError
- `test_connection_refused` — URLError → exception propagates
- `test_custom_base_url` — passes custom URL correctly

#### TestFallbackChain (6 tests)
- `test_first_provider_success` — gemini_cli succeeds → returns (text, "gemini_cli")
- `test_fallback_to_second` — cli fails, sdk succeeds → returns (text, "gemini_sdk")
- `test_fallback_to_third` — cli+sdk fail, ollama succeeds → returns (text, "ollama")
- `test_all_fail` — all 3 fail → RuntimeError
- `test_error_messages_collected` — all fail → error message contains all 3 errors
- `test_returns_tuple` — verify (str, str) return type

#### TestAnalyzerHandlerValidation (5 tests)
- `test_max_papers_zero_returns_422` — 0 → 422
- `test_max_papers_over_100_returns_422` — 101 → 422
- `test_max_papers_string_returns_422` — "ten" → 422
- `test_no_papers_returns_empty_summary` — empty DB → 0 analyzed
- `test_missing_title_skips_paper` — paper with no title → error in response

#### TestParseLlmResponse (4 tests)
- `test_valid_json` — returns parsed dict
- `test_invalid_json_retries` — first invalid, retry valid → returns parsed
- `test_invalid_json_both_fail` — both invalid → returns None, error added
- `test_retry_on_runtime_error` — retry fallback_chain raises → returns None

#### TestScoreValidation (5 tests)
- `test_valid_scores_accepted` — all 0.0-1.0 → computed correctly
- `test_out_of_range_clamped` — 1.5 → 1.0, -0.1 → 0.0, clamped=True
- `test_wrong_keys_rejected` — missing key → error
- `test_extra_keys_rejected` — extra key → set mismatch → error
- `test_non_numeric_value` — "high" → ValueError → error

#### TestMigrateDb (3 tests)
- `test_adds_column` — column not present → adds it
- `test_idempotent` — run twice → no error
- `test_index_created` — verify idx_papers_prompt_version exists

### Step 3: tests/integration/test_analyzer_db.py — Integration Tests (~300 LOC)

#### TestMigrateDbIntegration (3 tests)
- `test_migrate_real_db` — real SQLite, verify column exists after migration
- `test_migrate_preserves_data` — existing rows unchanged after migration
- `test_prompt_version_stored` — after analyze, prompt_version in DB

#### TestAnalyzerHandlerIntegration (8 tests)
Real HTTP server (BaseService in thread) + real SQLite, mock LLM only.

- `test_analyze_single_paper` — 1 discovered paper → stage='analyzed', score>0
- `test_reject_low_score` — low scores → stage='rejected'
- `test_threshold_boundary_exact` — score=0.7 exactly → 'analyzed'
- `test_threshold_boundary_below` — score=0.699 → 'rejected'
- `test_force_reprocess` — stage='analyzed' + force=true → re-scored
- `test_specific_paper_id` — paper_id param → only that paper
- `test_batch_mixed_results` — 3 papers: 1 accepted, 1 rejected, 1 error → summary correct
- `test_all_llm_fail` — fallback_chain raises → errors in response, no crash

#### TestScoreStorage (3 tests)
- `test_score_persisted` — after /process, score in DB matches expected
- `test_prompt_version_stored` — prompt_version="v1" in DB
- `test_clamped_flag_stored` — out-of-range score → error="score_clamped" in DB

### Step 4: tests/e2e/test_analyzer_e2e.py — E2E Tests (~80 LOC)

Real Ollama calls (localhost:11434). Skip if unavailable.

- `test_ollama_scores_paper` — real paper → valid scores JSON → overall 0.0-1.0
- `test_full_pipeline_discover_then_analyze` — insert paper → analyze → check DB
- `test_rejected_paper_stays_rejected` — low-relevance paper → stage='rejected'

## Verification

After implementation:
1. `pytest tests/unit/test_analyzer.py -v` — all unit tests pass
2. `pytest tests/integration/test_analyzer_db.py -v` — all integration tests pass
3. `pytest tests/e2e/test_analyzer_e2e.py -v -m e2e` — E2E tests pass (requires Ollama)
4. `pytest tests/ -x` — full suite, no regressions (excludes e2e by default)
5. `pytest tests/ --cov=services/analyzer --cov-report=term-missing` — coverage report

## Risk Assessment

| Risk | Mitigation |
|------|-----------|
| google-genai not importable in test | Mock the import path |
| Ollama not running for E2E | pytest.skip() if connection refused |
| Flaky LLM responses in E2E | Assert structure, not exact values |
| subprocess mock complexity | Use MagicMock with spec=subprocess.CompletedProcess |

## Acceptance Criteria

- [ ] Unit tests: 46+ tests, all passing
- [ ] Integration tests: 14+ tests, all passing
- [ ] E2E tests: 3+ tests, passing with real Ollama
- [ ] No regressions: existing 103+ tests still pass
- [ ] Coverage: services/analyzer/ ≥ 90% line coverage
- [ ] All 3 LLM providers tested (mock)
- [ ] Fallback chain tested (all scenarios)
- [ ] Score threshold boundary tested
- [ ] Migration idempotency tested
