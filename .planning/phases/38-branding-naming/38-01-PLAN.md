# Plan 38-01: PePeRS Branding & Rename

## Goal

Rename the project from "research-pipeline" to "PePeRS" (Paper Extraction, Processing, Evaluation, Retrieval & Synthesis) across all files, rename the filesystem directory, create professional README with arcade/gaming branding.

## Pre-conditions

- v11.0 complete, all 728+ tests passing
- No running services depend on the current path (services run via Docker or systemd — systemd units will be updated)
- Env prefix `RP_` stays unchanged (user decision)

## Tasks

### Task 1: Update pyproject.toml
- Change `name = "research-pipeline"` → `name = "pepers"`
- Update description to mention PePeRS

### Task 2: Update docker-compose.yml
- Change `name: research-pipeline` → `name: pepers`

### Task 3: Update Dockerfile
- Update comment referencing project name

### Task 4: Update systemd service files
- All 6 `.service` files in `deploy/`: update `WorkingDirectory`, `ExecStart`, `ReadWritePaths`
- Path: `/media/sam/1TB/research-pipeline` → `/media/sam/1TB/pepers`
- Update `rp-pipeline.target` description

### Task 5: Update documentation
- `ARCHITECTURE.md` (root): replace "Research Pipeline" references with "PePeRS"
- `docs/ARCHITECTURE.md`: same if exists separately
- `docs/RUNBOOK.md`: update references
- `.planning/` docs: update PROJECT.md title/description

### Task 6: Create README.md
- Professional README with PePeRS branding (arcade/gaming inspired)
- Sections: What it is, Features, Architecture overview, Quick Start, Configuration, API Endpoints, License
- ASCII art logo
- Mascotte concept: Pepe scientist frog

### Task 7: Audit all references (confidence gate feedback)
- `grep -r "research.pipeline\|research_pipeline\|Research.Pipeline" --include="*.py" --include="*.toml" --include="*.yml" --include="*.service" --include="*.md" --include="*.json" --include="*.target"` (include *.md for docs)
- Check Python imports for any "research_pipeline" references
- Check git remote URL
- Verify no absolute paths in config files beyond systemd

### Task 8: Clean up old artifacts
- Remove `research_pipeline.egg-info/` directory
- Run `uv sync` to regenerate with new name

### Task 9: Run ALL tests (confidence gate feedback: include e2e)
- `uv run python -m pytest tests/unit/ tests/integration/ -x -q` (668+ tests)
- Optionally run non-service e2e tests too
- All tests must pass

### Task 10: Rename filesystem directory (LAST)
- `mv /media/sam/1TB/research-pipeline /media/sam/1TB/pepers`
- Update git remote if needed (formal task, not just a note)
- Session will need restart after this

## Verification

- [ ] `grep -r "research-pipeline" --include="*.py" --include="*.toml" --include="*.yml" --include="*.service" --include="*.md" --include="*.json"` returns 0 results (excluding .planning/ history)
- [ ] `grep -r "research_pipeline" --include="*.py"` returns 0 results
- [ ] `uv sync` succeeds with new package name
- [ ] All unit + integration tests pass
- [ ] README.md exists with PePeRS branding
- [ ] Git remote is accessible from new directory

## Risks

- **Directory rename while session active**: CLI working directory becomes invalid. Mitigation: do this last, session can be restarted.
- **systemd units reference absolute paths**: Update all 6 service files + target. Mitigation: grep verification covers *.service and *.target.
- **Git remote**: After `mv`, git remote still works (path-independent). GitHub remote URL unchanged.

## Estimated LOC

- ~50 LOC changes across config files
- ~200 LOC new README.md
- 0 Python logic changes
