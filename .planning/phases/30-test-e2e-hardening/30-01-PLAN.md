# Plan 30-01: E2E Hardening Regression Tests

## Goal

Regression tests verifying Phase 28-29 fixes: stage transitions, batch overflow, formula filtering. One new integration test file + one new E2E test file.

## Pre-Conditions

- 582 non-e2e + 43 e2e tests passing
- Phase 28-29 code complete
- Existing unit tests for `is_nontrivial()` (14) and `clean_latex()` (20) — no duplication needed

## Deliverables

### File 1: `tests/integration/test_hardening.py` (~350 LOC)

New integration test file with real DB, mocked external services.

#### Class 1: TestStageTransitions (~120 LOC)

**Purpose**: Verify paper.stage advances correctly through pipeline stages.

| Test | What It Verifies |
|------|-----------------|
| `test_validator_updates_paper_stage` | Paper at "extracted" → validator processes → paper.stage = "validated" |
| `test_codegen_updates_paper_stage` | Paper at "validated" → codegen processes → paper.stage = "codegen" |
| `test_validator_no_update_on_all_failed` | All formulas fail → paper.stage stays "extracted" |
| `test_codegen_no_update_on_all_failed` | All formulas fail codegen → paper.stage stays "validated" |
| `test_full_stage_progression` | Paper goes extracted→validated→codegen with DB verification at each step |

**Implementation**:
- Use `extracted_formula_db` / `validated_formula_db` fixtures
- Mock CAS calls for validator (`_call_cas_service`)
- Mock LLM + SymPy for codegen (`parse_formula`, `call_llm`)
- Verify with `SELECT stage FROM papers WHERE id = ?`

#### Class 2: TestBatchIteration (~120 LOC)

**Purpose**: Verify orchestrator batch loop processes all formulas.

| Test | What It Verifies |
|------|-----------------|
| `test_batch_processes_all_formulas` | 75 formulas: orchestrator calls validator 2x (50+25), all processed |
| `test_batch_merge_sums_counters` | Merged result has correct totals across iterations |
| `test_batch_stops_at_zero` | Loop exits when `formulas_processed == 0` |
| `test_batch_safety_cap` | Mock infinite work → loop stops at 100 iterations, warning logged |
| `test_batch_partial_failure` | First batch OK, second fails → results merged, errors captured |
| `test_clean_latex_in_codegen_flow` | Formula with \tag{N} → clean_latex strips it → codegen succeeds |

**Implementation**:
- Seed DB with 75 formulas in "extracted" stage for one paper
- Mock `requests.post` to return batch results (50, then 25, then 0)
- For safety cap: mock to always return `formulas_processed: 1`
- Verify `_merge_batch_results` output counters

#### Class 3: TestResolveStages (~50 LOC)

**Purpose**: Verify stage resolution for edge cases.

| Test | What It Verifies |
|------|-----------------|
| `test_rejected_paper_returns_empty` | Paper with stage="rejected" → empty stage list |
| `test_failed_paper_returns_empty` | Paper with stage="failed" → empty stage list |
| `test_paper_at_codegen_returns_empty` | Paper already at final stage → empty (no stages left) |
| `test_paper_at_extracted_starts_from_validator` | stage="extracted" → starts at validator |

**Implementation**:
- Seed papers at various stages
- Call `runner._resolve_stages(None, paper_id, 5)`
- Assert returned stage list

#### Class 4: TestFilteredFormulasNoInfiniteLoop (~60 LOC)

**Purpose**: Verify trivial formulas don't create infinite batch loops.

| Test | What It Verifies |
|------|-----------------|
| `test_trivial_formulas_filtered_at_extraction` | Fragments like `\alpha`, `^{1}` don't reach "extracted" stage |
| `test_batch_terminates_with_no_extractable_formulas` | All formulas trivial → validator gets 0, batch exits immediately |

**Implementation**:
- Seed formulas through extractor's `filter_formulas()`
- Verify only nontrivial formulas stored in DB
- Run batch with 0 eligible formulas → verify 1 iteration, 0 processed

### File 2: `tests/e2e/test_pipeline_e2e.py` (~200 LOC)

New E2E test for full pipeline stage progression with real services (in-process).

#### Class: TestPipelineStageProgression (~200 LOC)

| Test | What It Verifies |
|------|-----------------|
| `test_paper_flows_through_all_stages` | Paper + formulas progress: extracted → validated → codegen, all DB states correct |
| `test_multiple_papers_independent_stages` | 2 papers at different stages, each advances independently |
| `test_batch_overflow_real_services` | 60+ formulas processed via real validator+codegen (mocked CAS/LLM) |
| `test_all_formulas_fail_stage_not_advanced` | All formulas fail → paper.stage NOT advanced (negative path) |

**Implementation**:
- Start validator + codegen services on random ports (thread-based)
- Mock only CAS HTTP calls and LLM calls (not the services themselves)
- Seed DB with paper at "extracted" + formulas
- POST /process to validator → check DB
- POST /process to codegen → check DB
- Verify complete state at each stage

## Test Markers

```python
@pytest.mark.integration  # For test_hardening.py
@pytest.mark.e2e         # For test_pipeline_e2e.py
```

## New Fixtures Needed (in conftest.py)

```python
@pytest.fixture
def multi_formula_db(initialized_db, sample_paper_row):
    """DB with one paper + N extracted formulas for batch testing."""
    # Paper at "extracted" stage, 75 nontrivial formulas
```

## Estimated LOC

| File | LOC |
|------|-----|
| `tests/integration/test_hardening.py` | ~350 |
| `tests/e2e/test_pipeline_e2e.py` | ~200 |
| `tests/conftest.py` (new fixture) | ~25 |
| **Total** | **~575** |

## Acceptance Criteria

- [ ] All 625 existing tests still pass
- [ ] 15+ new integration tests pass
- [ ] 3+ new E2E tests pass
- [ ] Stage transition regression: validator→"validated", codegen→"codegen" verified
- [ ] Batch overflow regression: >50 formulas processed in multiple iterations
- [ ] Safety cap: batch loop stops at MAX_BATCH_ITERATIONS
- [ ] 0 type errors (mypy)
