# Plan 16-01: Validator Service Test Suite

## Goal
Complete test suite for Validator Service: unit tests (consensus logic, CAS client), integration tests (real SQLite + mock CAS), E2E tests (real CAS service).

## Success Criteria
- [ ] All consensus decision matrix combinations covered (9 cases)
- [ ] CASClient validate/health/error paths tested
- [ ] DB query, store, update, fail functions tested with real SQLite
- [ ] HTTP endpoint `/process` tested end-to-end
- [ ] E2E with real CAS service (SymPy + Maxima)
- [ ] All 296 existing tests still pass
- [ ] Zero type errors (mypy)

## Tasks

### Task 1: Unit Tests — `tests/unit/test_validator.py`

Test consensus.py (pure logic, no mocks needed):
- `test_all_valid` — both engines valid → VALID
- `test_all_invalid` — both engines invalid → INVALID
- `test_valid_invalid_disagreement` — SymPy valid, Maxima invalid → INVALID
- `test_invalid_valid_disagreement` — SymPy invalid, Maxima valid → INVALID
- `test_valid_error` — one valid, one error → PARTIAL
- `test_error_valid` — one error, one valid → PARTIAL
- `test_invalid_error` — one invalid, one error → PARTIAL
- `test_error_invalid` — one error, one invalid → PARTIAL
- `test_all_error` — both error → UNPARSEABLE
- `test_single_engine_valid` — 1 engine, valid → VALID
- `test_single_engine_error` — 1 engine, error → UNPARSEABLE
- `test_three_engines_consensus` — 3 engines, majority logic
- `test_empty_results` — 0 engines → UNPARSEABLE

Test cas_client.py (mock urllib):
- `test_validate_success` — parse JSON response, return CASResponse
- `test_validate_http_error` — HTTPError → CASServiceError
- `test_validate_url_error` — URLError → CASServiceError (service down)
- `test_validate_with_engines` — engines parameter passed in payload
- `test_health_ok` — 200 + status=ok → True
- `test_health_down` — URLError → False
- `test_health_bad_status` — 200 but status != ok → False
- `test_base_url_trailing_slash` — trimmed correctly

### Task 2: Integration Tests — `tests/integration/test_validator_db.py`

Real SQLite, mock CAS HTTP:
- `test_query_formulas_extracted` — finds extracted formulas
- `test_query_formulas_by_paper_id` — paper_id filter
- `test_query_formulas_by_formula_id` — formula_id filter
- `test_query_formulas_force` — includes already-validated
- `test_query_formulas_limit` — respects max_formulas
- `test_query_formulas_empty` — no formulas → empty list
- `test_store_validations` — INSERT into validations table
- `test_store_validations_overwrites` — DELETE+INSERT for same formula+engine
- `test_update_formula_stage_valid` — stage → 'validated'
- `test_update_formula_stage_unparseable` — no change (stays 'extracted')
- `test_mark_formula_failed` — stage → 'failed', error message stored
- `test_process_endpoint_success` — full HTTP flow with mock CAS
- `test_process_endpoint_cas_down` — 503 when CAS unhealthy
- `test_process_endpoint_empty_formulas` — no work → counts all zero

### Task 3: E2E Tests — `tests/e2e/test_validator_e2e.py`

Real CAS service at :8769:
- `test_cas_health` — CAS service responds healthy
- `test_validate_simple_equation` — `x^2 + 2x + 1 = (x+1)^2`
- `test_validate_derivative` — `\frac{d}{dx} x^2 = 2x`
- `test_validate_invalid_formula` — gibberish → engines report invalid/error
- `test_full_process_flow` — insert formula in DB → POST /process → verify validations table + stage transition

### Task 4: Conftest additions

- `extracted_formula_db` fixture — DB with paper + extracted formula ready for validation
- `sample_cas_response` fixture — mock CAS API response dict

### Task 5: Verify & Regress

- Run full test suite: `pytest tests/ -v`
- Verify 0 regressions on existing 296 tests
- Run mypy type check

## File Changes

| File | Action |
|------|--------|
| `tests/unit/test_validator.py` | CREATE |
| `tests/integration/test_validator_db.py` | CREATE |
| `tests/e2e/test_validator_e2e.py` | CREATE |
| `tests/conftest.py` | EDIT (add fixtures) |

## Dependencies
- Phase 15 complete (Validator Service implemented)
- CAS service running at :8769
