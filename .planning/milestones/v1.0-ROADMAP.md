# Milestone v1.0: Foundation

**Status:** SHIPPED 2026-02-10
**Phases:** 1-4
**Total Plans:** 4

## Overview

Shared infrastructure library that all 5 microservices + orchestrator will depend on. Replaces the failed N8N W1-W5 research paper pipeline foundation.

## Phases

### Phase 1: Research & Design

**Goal**: Analyze CAS microservice (:8769) as reference pattern, design shared lib architecture
**Depends on**: Nothing (first phase)
**Plans**: 1 plan

Plans:
- [x] 01-01: Analyze CAS, create shared lib skeleton, write ARCHITECTURE.md

**Details:**
- Analyzed CAS microservice (405 LOC) — kept http.server, improved route dispatch, structured errors, logging, config
- Created shared/ package: db.py, models.py, server.py, config.py (all stubs with type hints)
- Comprehensive ARCHITECTURE.md (8 sections, ASCII data flow, API contracts)
- Key decisions: @route decorator, RP_ env prefix, WAL mode SQLite, ports 8770-8775

### Phase 2: Database & Models

**Goal**: SQLite database layer with clean schema + Pydantic models
**Depends on**: Phase 1
**Plans**: 1 plan

Plans:
- [x] 02-01: SQLite schema + Pydantic models + DB layer implementation

**Details:**
- SQLite DB layer: get_connection (WAL, FK, Row factory), transaction (context manager), init_db (idempotent)
- 5 tables: papers (22 cols), formulas (10), validations (8), generated_code (8), schema_version (2)
- 6 indexes for common query patterns
- 8 Pydantic models with JSON field validators for SQLite TEXT round-trips
- Formula dedup via latex_hash SHA-256

### Phase 3: HTTP Server & Config

**Goal**: Base HTTP server class + centralized config management
**Depends on**: Phase 2
**Plans**: 1 plan

Plans:
- [x] 03-01: Base HTTP server (route dispatch, JSON helpers, SIGTERM) + config loader

**Details:**
- BaseHandler: route dispatch, send_json, send_error_json, read_json, logging override
- BaseService: service metadata injection, /health + /status built-in, serve_forever
- JsonFormatter for structured logging (Loki/journald)
- Config loader: RP_{SERVICE}_{FIELD} env vars with defaults and warnings
- Graceful SIGTERM drain, thread-safe signal registration

### Phase 4: Test Suite

**Goal**: Comprehensive test suite for shared library
**Depends on**: Phase 3
**Plans**: 1 plan

Plans:
- [x] 04-01: Comprehensive test suite (103 tests, 98% coverage)

**Details:**
- 103 tests: 92 unit + 11 integration
- 98% coverage (server.py run() lifecycle uncovered — 6 lines)
- 0 Pyright type errors
- SQLite in-memory for unit, file-based for integration
- Free port allocation for HTTP tests
- Fixed requires-python >=3.10 (system Python 3.10.12)

---

## Milestone Summary

**Key Decisions:**
- Route dispatch via @route decorator (not monolithic do_POST)
- Standard error format with codes for AI agent consumption
- RP_ env var prefix for configuration
- WAL mode SQLite for concurrent reads
- Ports 8770-8775 assigned to services
- Maximal schema: all Semantic Scholar/CrossRef enrichment fields
- Formula dedup via latex_hash SHA-256, no M:N table
- JSON structured logging for Loki/journald
- Warn + default for missing env vars (development-friendly)
- SQLite in-memory for unit tests, file for integration

**Issues Resolved:**
- Python 3.10.12 vs 3.11 compatibility — fixed with requires-python >=3.10
- Confidence gate verifier hallucinations — proceeded on plan quality

**Issues Deferred:**
- server.py run() method coverage (6 lines, requires process-level SIGTERM testing)
- ARCHITECTURE.md location (in .planning/ not project root — by design)

**Technical Debt Incurred:**
- None significant

**Stats:**
- 36 files created
- ~2091 lines of Python
- 4 phases, 4 plans
- 1 day (2026-02-10)
- Git range: 3ee2602 → e3b3ba1

---

_For current project status, see .planning/ROADMAP.md_
